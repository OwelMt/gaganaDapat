# DAFAC family ages

Preserve the existing DAFAC layout and data schema. Existing family member age editing, Excel import, API persistence and PDF export are already supported. Expose the stored family head age in the editor and saved card. Show age for all family members in the existing wrapping chips, including members beyond the former two-name preview. Preserve zero for infants and distinguish missing display values. Add card display and editor save regression coverage, then run relief tests and verify rendered layout.
